services:
  - type: web
    name: voucher-pro
    env: docker
    plan: free
    dockerCommand: "npx tsx server.ts"
    envVars:
      - key: NODE_ENV
        value: production
    disk:
      name: data
      mountPath: /app/data
      sizeGB: 1
